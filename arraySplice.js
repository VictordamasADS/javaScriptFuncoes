// splice = emendar/unir
// ela substitui as outras funções: unshift, push, shift, pop.

const nomes = ['João', 'Victor', 'Kaly', 'Julia', 'Marie']

//unshift = acrescenta o nome no inicio
//const removidos = nomes.splice(0, 0, 'Elle');

//push = acrescenta o nome no final
//const removidos = nomes.splice(nomes.length, 0, 'Elle');

//shift = remove do inicio
//const removidos = nomes.splice(0, 1);

//pop = remove do final
//const removidos = nomes.splice(4, 1);
console.log(nomes, removidos)